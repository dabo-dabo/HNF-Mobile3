name: Build HNF Android APK
on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v4

      # 1. إعداد بايثون
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      # 2. إعداد الجافا
      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # 3. تثبيت متطلبات النظام (تمت إضافة مكتبات هامة لمنع الأخطاء)
      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential git ffmpeg libsdl2-dev libsdl2-image-dev \
            libsdl2-mixer-dev libsdl2-ttf-dev libportmidi-dev libswscale-dev \
            libavformat-dev libavcodec-dev zlib1g-dev libffi-dev libssl-dev \
            unzip autoconf libtool pkg-config automake libltdl-dev

      # 4. تثبيت Buildozer و Cython (الإصلاح هنا 🛠️)
      # Cython 3.0 يسبب فشل البناء، لذا نستخدم 0.29.36
      - name: Install Buildozer & Cython
        run: |
          pip install --upgrade pip
          pip install buildozer virtualenv "cython==0.29.36"

      # 5. بدء البناء
      - name: Build APK
        run: |
          yes | buildozer android debug

      # 6. رفع التطبيق
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: HNF-App-APK
          path: bin/*.apk
